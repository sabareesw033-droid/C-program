#include <stdio.h>
int main() {
    
    /* simple condition if-else */
    int A=100;
    int B=100;

    if (A>B)
    printf("A = %d and B=%d hence A is greater B",A,B);

    else if (A<B)
    printf("A =%d and B= %d hence A is smaller B",A,B);

    else
    printf("A =%d and B= %d hence A & B are equal smaller B",A,B);
}